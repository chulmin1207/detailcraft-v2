/**
 * 배경 장식 효과 컴포넌트
 * 화면 전체에 고정(fixed)된 반투명 그라데이션 원형 이펙트를 표시한다.
 * ::before 의사 요소는 index.css에서 처리하므로, 여기서는 bg-effects 클래스만 적용한다.
 */
export function BgEffects() {
  return <div className="bg-effects" />;
}
